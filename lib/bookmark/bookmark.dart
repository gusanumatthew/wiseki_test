import 'package:flutter/material.dart';

class BookMap extends StatelessWidget {
  const BookMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('bookMap'),
      ),
    );
  }
}
